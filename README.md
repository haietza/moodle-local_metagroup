The Metagroup plugin allows teachers who are using course meta links and groups (which are automatically synced through the enrolment plugin) to automatically sync metacourse enrolments into a metacourse group.

# Installation instructions
Install the plugin according to the [MoodleDocs](https://docs.moodle.org/en/Installing_plugins) as a directory named metagroup in the local directory.